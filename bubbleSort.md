# 冒泡排序 Bubble Sort

## 算法原理
通过遍历待排序的序列，比较相邻元素，交换较大元素的位置至右侧，或者交换较小元素的位置至左侧。  
在遍历过程中使较大元素逐步“冒出”至序列右侧。  

## 核心思想
1. 相邻比较  
	每次比较相邻的两个元素  
2. 交换位置  
	若不满足升序或者降序，则交换元素的位置  
3. 多轮遍历  
	通过多轮遍历确保所有元序有序排列  
4. 逐步沉浮  
	每轮遍历使当轮最大的元素上浮至顶，或者使当轮最小的元素下沉至底  

## 复杂度分析
- 复杂用时  
	数组完全逆序，对每个元素重新排序，用时 $O(n^2)$   
- 简单用时  
	数组完全有序，遍历每个元素但无需重排，用时 $O(n)$   
- 平均用时  
	用时 $O(n^2)$   
- 空间复杂度  
	原地排序，无需额外空间，占用 $O(1)$   